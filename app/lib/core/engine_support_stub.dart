/// Implémentation web (pas de dart:io) : pas de version système exploitable,
/// on laisse les appels natives échouer de façon descriptible par ailleurs.
String? localEngineUnsupportedReason() => null;